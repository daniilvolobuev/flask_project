# 1. Берем базовый образ
FROM python:3.9-slim

# 2. Указываем рабочую директорию внутри контейнера
WORKDIR /code

# 3. Копируем файл со списком библиотек
COPY requirements.txt .

# 4. Устанавливаем библиотеки (это произойдет один раз при сборке!)
RUN pip install --no-cache-dir -r requirements.txt

# 5. Копируем остальной код (app.py)
COPY . .

# 6. Команда по умолчанию для запуска приложения
CMD ["python", "-m", "flask", "run", "--host=0.0.0.0"]
