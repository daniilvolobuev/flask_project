name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

deploy:
    runs-on: self-hosted  
    needs: build-and-push
    steps:
      - name: Update containers
        run: |
          cd /home/kDst9/simpleapp  
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          docker compose pull
          docker compose up -d
          docker system prune -f
